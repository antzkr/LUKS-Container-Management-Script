LUKS Container Management Script
Changelog:~


v4.1
Redesigned shrinking size calculations, clearer shrinking guide, updated container info function.

v4.0
Changed broken size calculations, changed selection prompt from shrinking container size to filesystem size

v3.9
Improved shrink minimum size calculation - uses filesystem size + LUKS overhead + safety buffer instead of just data content size; added detailed size analysis display before shrink; better protection against accidental data loss

v3.8
Fixed LUKS2 keyslot parsing - exact matching for "Keyslots:" section header, skip "Data segments:" section to avoid false slot matches, improved regex to avoid matching non-keyslot sections

v3.7
Fixed keyslot display caching issues, fixed menu return after password change, fixed output alignment in container info display

v3.6
Fixed critical syntax error (missing newline before echo), added keyfile size validation, improved error_detach to reset global variables, standardized escape codes, added integer overflow protection, fixed typos, improved LUKS version caching

v3.5
Fixed return value error in display_keyslots, using global variable instead

v3.4
Fixed keyslot display for LUKS2 format, added safety checks for keyslot removal

v3.3
Added centralized display_keyslots function, improved LUKS2 parsing

v3.2
Added keyfile support, header backup, password change, and keyslot management

v3.1
Fixed LUKS size verification logic - read size BEFORE cryptsetup resize

v3.0
Fixed file expansion with proper fallocate/dd verification and fallback

v2.9
Fixed loop device refresh after file expansion

v2.1
Disk space and filesystem checks, improved error handling, container backup verification, logic rewrite, clearer comments.

v2.0
Colorized menus, added option to shrink or expand existing container, visual tweaks.

v1.2

Public release. Cleanup on exit trap, validation checks, required packaged installed check.
